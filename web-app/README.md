# GitCanvas Web App

Separate responsive frontend for GitCanvas.

Open `index.html` directly in a browser to preview the current first-pass UI.

Structure:
- `frontend/` - HTML, CSS, and client-side app logic
- `backend/` - future proxy or deployment backend
- `ai/` - future AI features
- `themes/` - theme registry and assets
- `shared/` - shared utilities

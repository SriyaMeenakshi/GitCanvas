# Backend

This folder is reserved for the standalone web app backend that will eventually proxy GitCanvas API requests, handle auth-backed cards, and support deployment-specific logic.

Planned pieces:
- API proxy
- token-safe headers
- image conversion or caching
- deployment environment config
